server {
    listen 5173;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Serve remoteEntry.js (Traefik strips /customers prefix)
    location = /remoteEntry.js {
        alias /usr/share/nginx/html/assets/remoteEntry.js;
        add_header Content-Type application/javascript;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Serve assets (Traefik strips /customers prefix)
    location /assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Serve CSS files from assets (Traefik strips /customers prefix)
    # Module Federation may request CSS files directly like /style-XXX.css
    # Map /style-XXX.css to /assets/style-XXX.css
    location ~ ^/style-(.+\.css)$ {
        alias /usr/share/nginx/html/assets/style-$1;
        add_header Content-Type text/css;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Handle /customers without trailing slash (when accessed directly, not through Traefik)
    location = /customers {
        return 301 $scheme://$host/customers/;
    }

    # Serve index.html and other files for /customers/ path (when accessed directly)
    location /customers/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html;
    }

    # Fallback for root access - serve index.html directly
    location / {
        try_files $uri $uri/ /index.html;
    }
}


